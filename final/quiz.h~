#ifndef QUIZ_H
#define QUIZ_H

#include<vector>
#include "question.h"


class Quiz
{
	public:
		Quiz();

		void getquiz(std::string filename);
		void displayq(int i);

		void unread();
		void secondtime();
		void printscore(std::string filename);

	private: 
		std::vector<Question> m_list;
};

#endif
